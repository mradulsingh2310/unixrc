-- ┌──────────────────────────────────────────────────────────────┐
-- │  NEOVIM CONFIG - Synced to: ~/unixrc/nvim/                   │
-- │  After editing, run: cp -r ~/.config/nvim/* ~/unixrc/nvim/   │
-- │  Then commit: cd ~/unixrc && git add -A && git commit -m "update nvim" && git push
-- └──────────────────────────────────────────────────────────────┘

-- bootstrap lazy.nvim, LazyVim and your plugins
require("config.lazy")
